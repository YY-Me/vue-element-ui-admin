server:
  port: 18080
  tomcat:
    threads:
      max: 1000
  shutdown: graceful
spring:
  application:
    name: boot-auth
  profiles:
    active: dev,other
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB
  mvc:
    servlet:
      load-on-startup: 1
