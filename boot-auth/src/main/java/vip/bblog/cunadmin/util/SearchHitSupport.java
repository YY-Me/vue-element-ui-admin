package vip.bblog.cunadmin.util;

/**
 * @author <a href="1396513066@qq.com">Yu Yong</a>
 * @version 1.0
 * @date 2020年05月29日 15:26
 * @desc SearchHitSupport 4.0后的解析
 */
public class SearchHitSupport {

    /*public static <T> List<T> unwrapSearchHits(SearchHits<T> result) {
        if (result == null) {
            return Collections.emptyList();
        }
        List<SearchHit<T>> searchHits = result.getSearchHits();
        return searchHits.stream()
                .map(SearchHitSupport::unwrapSearchHit)
                .collect(Collectors.toList());
    }

    public static <T> T unwrapSearchHit(SearchHit<T> result) {
        return result.getContent();
    }*/

}
